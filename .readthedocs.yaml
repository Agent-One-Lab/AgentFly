version: 2

submodules:
  include: all
  recursive: true
  
build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: agents/requirements.txt
    - requirements: verl/requirements.txt
    - requirements: docs/requirements-docs.txt
